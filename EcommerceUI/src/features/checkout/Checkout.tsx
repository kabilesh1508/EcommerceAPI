import { Typography } from "@mui/material";

export default function Checkout(){
    return(
        <Typography variant="h3">
            Only logged in users must be able to see this!!
        </Typography>
    )
}